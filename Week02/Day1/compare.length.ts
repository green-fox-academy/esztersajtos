// -  Create a variable named `firstList`
//    with the following content: `[1, 2, 3]`
// -  Create a variable named `secondList`
//    with the following content: `[4, 5]`
// -  Log to the console if `secondList` has more elements than `firstList`

let fisrtList: any = [1, 2, 3];
let secondList: any = [4, 5];
   
    if (secondList.length > fisrtList.length) {
        console.log('Secound List has more elements');
    } else {
        console.log('First List has more element');
    }
    